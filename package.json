{
    "name": "the-office-script-api",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "npm install --prefix server && npm install --prefix client"
    },
    "dependencies": {
        "axios": "^1.3.4",
        "dotenv": "^16.0.3"
    }
}
